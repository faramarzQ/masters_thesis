Heuristic scaler:

Cluster:
    Calculate energy consumption of a node during a specific time

Node Silencer:

Random scheduler:
    Problem: does not consider node scores

Proposed scaler:
    Implement RL model
        Persist epsilon value in DB
        Persist Selected action in DB
        